name: mcp_hub
description: Agent Hub — manage MCP, Skills and other Cursor/Codex agent configs.
publish_to: "none"
version: 0.1.20+0

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  path: ^1.9.1
  path_provider: ^2.1.6
  shared_preferences: ^2.5.5
  uuid: ^4.5.1
  mcp_dart: ^2.4.0
  webdav_client: ^1.2.2
  crypto: ^3.0.7
  archive: ^4.0.9
  package_info_plus: ^9.0.1
  file_selector: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
